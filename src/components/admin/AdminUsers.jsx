import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminUsers = () => {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h4" gutterBottom>
        Admin Users
      </Typography>
      <Typography variant="body1">
        This is a placeholder for the Admin Users component.
      </Typography>
    </Box>
  );
};

export default AdminUsers;